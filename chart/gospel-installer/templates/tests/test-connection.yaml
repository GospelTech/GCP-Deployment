apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gospel-installer.fullname" . }}-test-connection"
  labels:
{{ include "gospel-installer.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "gospel-installer.fullname" . }}:{{ .Values.frontendService.port }}']
  restartPolicy: Never
